package rcs;

import rcs.system.ShellCommandLineInterface;

public class TestRCS {
    public static void main(String[] args) {
        ShellCommandLineInterface shellCommandLineInterface =new ShellCommandLineInterface();
        shellCommandLineInterface.triggerMethod();
    }
}
